# portwatch-go
PortWatch is a lightweight Go-based TCP monitoring tool that checks IPv4/IPv6 ports, detects downtime, sends Discord notifications, and generates automatic MTR trace reports. Supports multiple targets, per-IP monitoring, timezone handling, log rotation, and parallel checks for fast, reliable uptime tracking.
